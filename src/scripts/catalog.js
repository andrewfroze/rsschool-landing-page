function renderCatalog() {
    const catalogContainer = document.createElement("h1");
    catalogContainer.textContent = "Catalog will be here";
    return catalogContainer;
}

export { renderCatalog }